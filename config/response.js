module.exports = Object.freeze({
    LOGIN:{
        success:{
            loginSuccess:"Login successfully."
        },
        failure:{
            invalidUsername: "Invalid user name",
            invalidPassword: "Invalid password",
            invalid:"User name or password invalid."
        }

    },
});